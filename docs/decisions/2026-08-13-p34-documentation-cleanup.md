# P2-4: Documentation and Report Cleanup

- Updated `README.md` with setup, daily operation, tests, and backtest sections.
- Updated `docs/ARCHITECTURE.md` to remove obsolete `fast.py`, `decision.py::run_decision`, and `sre.py` references.
- Added `daily` subcommand, `gap_store`, and `var/` output tree to architecture docs.
- Moved "不採用実験の記録" section from `AGENTS.md` to `docs/experiment_graveyard.md` and linked back.
